# Redirects for SEO optimization
# Ensure no redirect chains exist

# Redirect www to non-www (if you have both)
# www.yourdomain.com/* https://yourdomain.com/:splat 301

# Redirect old URLs to new ones (if any)
# /old-page /new-page 301

# Handle trailing slashes
/* /:splat 200

# Ensure proper canonical URLs
# No redirects that could create chains
