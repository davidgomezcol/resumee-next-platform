// Same card as the Open Graph image. Re-exported rather than duplicated: the two static PNGs this
// replaces were byte-identical and 396 kB each, and neither matched the 1.91:1 that social
// crawlers crop to.
export { default, alt, size, contentType } from './opengraph-image'
