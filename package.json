{
  "name": "nextjs-dev-portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@types/nodemailer": "^7.0.1",
    "next": "^15.1.12",
    "nodemailer": "^7.0.6",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "//browserslist": "Tailwind v4's own baseline. The stylesheet already relies on @property and color-mix(), so anything older cannot render this site correctly whatever the JS targets — which makes the polyfills Next was shipping for Array.prototype.at, Object.hasOwn and friends dead weight in every bundle.",
  "browserslist": [
    "chrome >= 111",
    "edge >= 111",
    "firefox >= 128",
    "safari >= 16.4",
    "ios_saf >= 16.4"
  ],
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.6",
    "@types/node": "^22",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^9",
    "eslint-config-next": "^15.1.12",
    "postcss": "^8",
    "prettier": "^3.5.1",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "tailwindcss": "^4.0.6",
    "typescript": "^5"
  }
}
